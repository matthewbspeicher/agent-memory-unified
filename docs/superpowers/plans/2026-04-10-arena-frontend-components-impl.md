# Arena React Components Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the React 19 frontend components for the AI Esports Arena, consuming real-time WebSocket events and styling them using the newly implemented `DESIGN.md` Token Pipeline.

**Architecture:** A master `ArenaMatchStream` component coordinates the TradingView-style price chart, the Agent "Stock" Cards, and the Parimutuel Prop Bet UI. All components strictly use Tailwind CSS variables generated by the Token Pipeline.

**Tech Stack:** React 19, Tailwind CSS, WebSockets.

---

### Task 1: Agent Stock Card (`AgentPriceCard.tsx`)

**Files:**
- Create: `frontend/src/components/Arena/AgentPriceCard.tsx`

- [ ] **Step 1: Write the minimal implementation**

```tsx
import React, { useState, useEffect } from 'react';
import { clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function AgentPriceCard({ agentName, currentPrice, previousPrice, isLying }: { agentName: string, currentPrice: number, previousPrice: number, isLying: boolean }) {
  const priceDiff = currentPrice - previousPrice;
  const isUp = priceDiff >= 0;
  
  return (
    <div className={twMerge(
      "glass-panel p-4 flex flex-col justify-between transition-all duration-500",
      isLying ? "shadow-glow-danger border-accent-danger/50" : "hover:border-border-subtle/20"
    )}>
      <div className="flex justify-between items-center">
        <h3 className="text-text-primary font-bold font-sans">{agentName}</h3>
        {isLying && <span className="text-xs text-accent-danger font-mono font-bold animate-pulse">LIAR DETECTED</span>}
      </div>
      <div className="mt-4 flex items-end gap-2">
        <span className="text-2xl text-text-primary font-mono">${currentPrice.toFixed(2)}</span>
        <span className={clsx(
          "text-sm font-mono mb-1",
          isUp ? "text-accent-success" : "text-accent-danger"
        )}>
          {isUp ? '+' : ''}{priceDiff.toFixed(2)}
        </span>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/Arena/AgentPriceCard.tsx
git commit -m "feat(arena): build AgentPriceCard consuming pipeline tokens"
```

---

### Task 2: Prop Bet Alert UI (`PropBetUI.tsx`)

**Files:**
- Create: `frontend/src/components/Arena/PropBetUI.tsx`

- [ ] **Step 1: Write the minimal implementation**

```tsx
import React, { useState, useEffect } from 'react';

export function PropBetUI({ prompt, secondsRemaining, onVote }: { prompt: string, secondsRemaining: number, onVote: (choice: boolean) => void }) {
  const [timeLeft, setTimeLeft] = useState(secondsRemaining);

  useEffect(() => {
    if (timeLeft <= 0) return;
    const timer = setInterval(() => setTimeLeft(t => t - 1), 1000);
    return () => clearInterval(timer);
  }, [timeLeft]);

  return (
    <div className="neural-card-accent border-accent-warning/30 shadow-glow-warning flex flex-col gap-4">
      <div className="flex justify-between items-center border-b border-border-subtle/10 pb-2">
        <h4 className="text-accent-warning font-bold uppercase tracking-wider text-sm">Live Prop Bet</h4>
        <span className="font-mono text-text-secondary">{timeLeft}s</span>
      </div>
      
      <p className="text-text-primary text-lg">{prompt}</p>
      
      <div className="flex gap-4 mt-2">
        <button 
          onClick={() => onVote(true)}
          disabled={timeLeft <= 0}
          className="flex-1 neural-button bg-accent-success/20 text-accent-success border border-accent-success/30 hover:bg-accent-success/30"
        >
          YES
        </button>
        <button 
          onClick={() => onVote(false)}
          disabled={timeLeft <= 0}
          className="flex-1 neural-button bg-accent-danger/20 text-accent-danger border border-accent-danger/30 hover:bg-accent-danger/30"
        >
          NO
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/Arena/PropBetUI.tsx
git commit -m "feat(arena): build PropBetUI with 60s timer"
```

---

### Task 3: The Master Stream View (`ArenaMatchStream.tsx`)

**Files:**
- Create: `frontend/src/components/Arena/ArenaMatchStream.tsx`

- [ ] **Step 1: Write the minimal implementation**

```tsx
import React, { useEffect, useState } from 'react';
import { AgentPriceCard } from './AgentPriceCard';
import { PropBetUI } from './PropBetUI';

// Mock WebSocket hook
function useArenaWebSocket() {
  const [events, setEvents] = useState<any[]>([]);
  const [prices, setPrices] = useState<Record<string, number>>({'Agent 1': 10.0, 'Agent 2': 10.0});
  
  useEffect(() => {
    // In reality, this connects to the Bookie backend
    const timer = setInterval(() => {
      setPrices(prev => ({
        ...prev,
        'Agent 1': prev['Agent 1'] + (Math.random() - 0.5),
        'Agent 2': prev['Agent 2'] + (Math.random() - 0.5)
      }));
    }, 2000);
    return () => clearInterval(timer);
  }, []);

  return { events, prices };
}

export function ArenaMatchStream() {
  const { events, prices } = useArenaWebSocket();

  return (
    <div className="min-h-screen bg-bg-base p-8 grid grid-cols-12 gap-8">
      
      {/* Left Column: Live Stream & Chat */}
      <div className="col-span-8 flex flex-col gap-6">
        <div className="glass-panel h-96 p-6 flex flex-col justify-end overflow-hidden relative">
           <div className="absolute top-4 left-4 text-text-secondary font-mono text-sm tracking-widest uppercase">Live Terminal Stream</div>
           {/* Terminal output goes here */}
           <div className="text-text-primary font-mono opacity-50">>>> Phase changed to: WerewolfState.NIGHT</div>
           <div className="text-text-primary font-mono opacity-80">>>> Phase changed to: WerewolfState.DAWN</div>
           <div className="text-accent-primary font-mono mt-2">[ACTION] Agent 1 executed inspect tool.</div>
        </div>
        
        {/* The TradingView Placeholder */}
        <div className="neural-card h-64 flex items-center justify-center border-chart-1/30 shadow-glow-primary">
           <span className="text-text-muted font-mono">[ TradingView Chart Component ]</span>
        </div>
      </div>

      {/* Right Column: Economy & Betting */}
      <div className="col-span-4 flex flex-col gap-6">
        <h2 className="text-2xl text-text-primary font-bold mb-2">Live Odds</h2>
        
        <div className="grid grid-cols-1 gap-4">
          <AgentPriceCard agentName="Agent 1" currentPrice={prices['Agent 1']} previousPrice={10.0} isLying={false} />
          <AgentPriceCard agentName="Agent 2" currentPrice={prices['Agent 2']} previousPrice={10.0} isLying={prices['Agent 2'] < 9.5} />
        </div>

        <div className="mt-8">
          <PropBetUI prompt="Will the Blue Team patch the server before the Red Team breaches it?" secondsRemaining={60} onVote={() => {}} />
        </div>
      </div>
      
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/Arena/ArenaMatchStream.tsx
git commit -m "feat(arena): build master ArenaMatchStream layout"
```
